# Build products
/.build/
/.dist/
*.o
*.swiftmodule

# macOS
.DS_Store

# Xcode / SwiftPM local state
/.swiftpm/
*.xcodeproj
Package.resolved

# AI assistant support files (not part of the public project)
/CLAUDE.md
/spec/
/docs/superpowers/

# Bundled diagram renderers — fetched at package time, not committed.
# (*.tmp are half-finished downloads left by an interrupted package_app.sh)
/vendor/mermaid/mermaid.min.js
/vendor/plantuml/plantuml.jar
/vendor/*/*.tmp
