## What this changes

<!-- The user-visible behavior change, in a sentence or two. -->

## Why

<!-- The problem it solves, or the issue it closes (Fixes #123). -->

## How it was verified

<!--
There is no automated coverage for the AppKit layer, so say what you actually
did: which panel, which shortcut, which backend. A screenshot helps for
anything visual.
-->

- [ ] `swift build` passes without new warnings
- [ ] `swift test` passes
- [ ] Exercised the change in the running app
- [ ] New user-visible strings go through `tr()`
